# Amelia 2.0 depends on is-apple-silicon from its upstream GitHub repository.
# npm 12 blocks Git dependencies by default; enable them only for this project.
allow-git=all

# The project allowlist in package.json approves only sharp@0.32.6. Enable that
# reviewed install script even if a user-level npm configuration disables all.
ignore-scripts=false
